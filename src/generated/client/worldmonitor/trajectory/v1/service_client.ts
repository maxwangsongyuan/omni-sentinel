// @ts-nocheck
// Code generated by protoc-gen-ts-client. DO NOT EDIT.
// source: worldmonitor/trajectory/v1/service.proto

export interface FlightHistoryRequest {
  icao24: string;
  begin: number;
  end: number;
}

export interface FlightHistoryResponse {
  points: TrajectoryPoint[];
  callsign: string;
  status: string;
  errorMessage: string;
}

export interface TrajectoryPoint {
  latitude: number;
  longitude: number;
  altitude: number;
  timestamp: number;
  velocity: number;
  heading: number;
  onGround: boolean;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface TrajectoryServiceClientOptions {
  fetch?: typeof fetch;
  defaultHeaders?: Record<string, string>;
}

export interface TrajectoryServiceCallOptions {
  headers?: Record<string, string>;
  signal?: AbortSignal;
}

export class TrajectoryServiceClient {
  private baseURL: string;
  private fetchFn: typeof fetch;
  private defaultHeaders: Record<string, string>;

  constructor(baseURL: string, options?: TrajectoryServiceClientOptions) {
    this.baseURL = baseURL.replace(/\/+$/, "");
    this.fetchFn = options?.fetch ?? globalThis.fetch;
    this.defaultHeaders = { ...options?.defaultHeaders };
  }

  async queryFlightHistory(req: FlightHistoryRequest, options?: TrajectoryServiceCallOptions): Promise<FlightHistoryResponse> {
    let path = "/api/trajectory/v1/query-flight-history";
    const params = new URLSearchParams();
    if (req.icao24 != null && req.icao24 !== "") params.set("icao24", String(req.icao24));
    if (req.begin != null && req.begin !== 0) params.set("begin", String(req.begin));
    if (req.end != null && req.end !== 0) params.set("end", String(req.end));
    const url = this.baseURL + path + (params.toString() ? "?" + params.toString() : "");

    const headers: Record<string, string> = {
      "Content-Type": "application/json",
      ...this.defaultHeaders,
      ...options?.headers,
    };

    const resp = await this.fetchFn(url, {
      method: "GET",
      headers,
      signal: options?.signal,
    });

    if (!resp.ok) {
      return this.handleError(resp);
    }

    return await resp.json() as FlightHistoryResponse;
  }

  private async handleError(resp: Response): Promise<never> {
    const body = await resp.text();
    if (resp.status === 400) {
      try {
        const parsed = JSON.parse(body);
        if (parsed.violations) {
          throw new ValidationError(parsed.violations);
        }
      } catch (e) {
        if (e instanceof ValidationError) throw e;
      }
    }
    throw new ApiError(resp.status, `Request failed with status ${resp.status}`, body);
  }
}
